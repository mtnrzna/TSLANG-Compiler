function find(Array A, Int x) returns Int:
    val Int n;
    i = 0;
    foreach(n of A):
        if (n == k):
            return i;
        end
        i = i +1;
        n(5);
    end
    return -1;
end
function main() returns Int:
    val Array A;
    val Int a;
    A = createArray(a);
    A[0] = 3;
    A[1] = 8;
    A[2] = 5;
    exit(a);
    printInt(find(A, a, A,22 ));
    printInt(find(A));
    printInt(find(A, a));
    return A;
end