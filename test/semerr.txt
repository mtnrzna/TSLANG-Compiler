function find(Array A, Int x) returns Int:
    val Array n;
    x = n;
    val Int x;
    i = 0;
    foreach(n of A):
        if (n == k):
            return i;

---------------------------------------------------------------------semantic errors
  --    #kjhfklsdhfjkd oinveir int jhdjhsd 
--
        end
        i = i +1;
        n(5);
    end
    return 0;
end
function main() returns Int:
    var = 8;
    val Int var;
    val Array A;
    val Int a;
    A = createArray(a);
    A[0] = 3;
    A[1] = 8;
    A[2] = 5;
    exit(a);
    printInt(find(A, a, A,22 ));
    printInt(find(A));
    printInt(find(A, A));
    return A;
end