function main() returns Int:
    
    val Int n;
    n = getInt();
    
    n = fib(n);
    printInt(n);
    return 0;
end

function fib(Int n) returns Int:

    if(n < 2) return 1;
    else
        return (fib(n-1) + fib (n-2));
end